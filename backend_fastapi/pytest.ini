[pytest]
# asyncio_mode = auto # or 'strict'
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests
    unit: marks tests as unit tests
# Add other configurations like environment variables for tests if needed
# Example:
# env =
#     APP_ENV=test
#     MONGODB_DB_NAME=test_my_fastapi_db
#     REDIS_DB=1
